name = "kyl-bug"
main = "bug-report.mjs"
compatibility_date = "2024-11-01"

[vars]
GH_REPO = "wardcrazy01894/KnowYourLocals"
# For local dev you can use "*"; in production set your Pages origin, e.g.
# ALLOWED_ORIGIN = "https://wardcrazy01894.github.io"
ALLOWED_ORIGIN = "*"

# GH_TOKEN is a SECRET — do not put it here. Set it with:
#   wrangler secret put GH_TOKEN
